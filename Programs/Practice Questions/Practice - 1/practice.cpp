#include <iostream>

using namespace std;

int main()
{

    // 1.

    // int n;
    // cin >> n;

    // int product = 1;
    // int sum = 0;

    // while (n)
    // {
    //     product = product * (n % 10);
    //     sum = sum + (n % 10);
    //     n = n / 10;
    // }

    // cout << product - sum;

    // 2.

    // int n;
    // cin >> n;
    // int hammingWeight = 0;

    // while (n)
    // {
    //     if (n % 10 != 0)
    //     {
    //         hammingWeight += 1;
    //     }
    //     n = n / 10;
    // }

    // cout << hammingWeight;

    return 0;
}