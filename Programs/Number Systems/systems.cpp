#include <iostream>
#include <string>
#include <math.h>
using namespace std;

int main()
{

    // 1. Decimal to bonary

    // int n;
    // cin >> n;
    // int ans = 0;
    // int i = 0;

    // while (n != 0)
    // {
    //     int bit = n & 1;
    //     ans = (bit * round(pow(10, i)) + ans);
    //     n = n >> 1;
    //     i++;
    // }

    // 2. Binary to decimal

    // int n;
    // cin >> n;

    // int ans = 0;
    // int i = 0;

    // while (n != 0)
    // {
    //     int bit = n % 10;
    //     if (bit)
    //         ans = ans + round(pow(2, i));
    //     i++;
    //     n = n / 10;
    // }

    // cout << ans;
}